package com.tahir.jtt1078.entity;

public class Audio extends Media
{
    public Audio(long sequence, MediaEncoding.Encoding encoding, byte[] data)
    {
        super(sequence, encoding, data);
    }
}
